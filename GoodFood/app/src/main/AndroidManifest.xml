<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.serujilituni.goodfood">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GoodFood">
        <activity android:name=".activities.intermediate.IntermediateActivity"></activity>
        <activity android:name=".activities.forgotpassword.ResetPasswordActivity" />
        <activity android:name=".activities.ordercomplete.OrderCompleteActivity" />
        <activity android:name=".activities.ordersummary.OrderSummaryActivity" />
        <activity android:name=".activities.menu.MenuActivity" />
        <activity android:name=".activities.restaurant.RestaurantsActivity" />
        <activity android:name=".activities.changedata.ChangeDataActivity" />
        <activity android:name=".activities.register.RegisterActivity" />
        <activity android:name=".activities.login.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>